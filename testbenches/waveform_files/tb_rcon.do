add wave -noupdate -group {Reset Controller} $rcon_path/clock
add wave -noupdate -group {Reset Controller} $rcon_path/activate_reset
add wave -noupdate -group {Reset Controller} $rcon_path/reset_out
add wave -noupdate -group {Reset Controller} $rcon_path/reset_done
add wave -noupdate -group {Reset Controller} $rcon_path/reset_done_delayed
add wave -noupdate -group {Reset Controller} $rcon_path/reset_done_sig
